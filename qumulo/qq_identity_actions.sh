    identity_attributes_get
                        Get attributes related to the given identity.
    identity_attributes_set
                        Set attributes related to the given identity.
    identity_config_expand_identity
                        Find equivalent identities and full group membership.
    identity_config_find_identity
                        Find all representations of an auth_id.
    identity_create_config
                        Create an identity config
    identity_delete_config
                        Delete an identity config
    identity_get_config
                        Get an identity config
    identity_list_configs
                        List all identity configs
    identity_modify_config
                        Modify an identity config